package chapter01.sector04

/**
 * var는 변수를 선언할 때 사용하는 키워드 (variable의 줄임말)
 * var는 내용을 다시 대입할 수 있음
 * var 식별자 = 값
 */

fun main() {
    var whole = 11
    var fractional = 1.4
    var words = "Twas Brilling"

    fractional += 3

    println(whole)
    println(fractional)
    println(words)
}